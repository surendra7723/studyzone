from django.apps import AppConfig


class DictionaryAppConfig(AppConfig):
    """Configuration for the dictionary lookup app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "dictionary_app"
